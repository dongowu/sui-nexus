[package]
name = "sui_nexus"
version = "0.1.0"
edition = "2024.beta"

[addresses]
# Replace "0x0" with the actual package address after `sui client publish`:
#   sui client publish --gas-budget 100000000
sui_nexus = "0x0"
